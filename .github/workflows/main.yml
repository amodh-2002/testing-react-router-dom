name: My first workflow
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        
      - name: Print Hello
        run: echo "Hello, GitHub Actions!" 
